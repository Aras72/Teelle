# راهنمای کوتاه پنل مدیریت بازی‌ها

## ورود به پنل

پس از ورود با حسابی که نقش `admin` یا `content_editor` دارد، گزینه «پنل مدیریت» در نوار بالای سایت نمایش داده می‌شود. نشانی مستقیم پنل:

`/admin/content`

## افزودن بازی

1. برای یک بازی تازه، «پیش‌نویس تازه» را بزنید.
2. متن، بازه سنی، وسایل، مراحل، ایمنی و منبع را وارد و ذخیره کنید.
3. تصویر را همراه متن جایگزین و نکته برش بارگذاری کنید.
4. پیش‌نویس را برای بازبینی بفرستید.
5. بازبین مستقل پنج حوزه متن، منبع، سن، ایمنی و تصویر را بررسی می‌کند.
6. فقط نسخه تأییدشده می‌تواند منتشر شود.

برای ورود گروهی از «Import بازی‌ها» استفاده می‌شود. دکمه «پیش‌نمایش Pilot پیشنهادی» ۲۵ بازی فعلی را ابتدا فقط به حالت Draft وارد می‌کند و هیچ بازی را خودکار منتشر نمی‌کند.

## فایل بررسی مالک

فایل `templates/Teelle_Game_Review_Template_v1.xlsx` شامل ۲۵ بازی فعلی است. در شیت «بازی‌ها» ستون‌های زرد را کامل کنید. ستون «سناریوی اصلی» فقط شناسه‌ای ثابت مثل `S01` می‌پذیرد. در شیت «سناریوها» شرایط از Dropdownهای ثابت انتخاب می‌شوند و برای هر وضعیت سه انتخاب برتر نوشته می‌شود. تعریف هر کلیدواژه در شیت «واژه‌نامه سناریو» آمده و تغییر تعریف، نسخه تازه فایل می‌خواهد. فایل تکمیل‌شده مبنای ورود تصویر، بازبینی انسانی و تنظیم Ranking خواهد بود.

سه ستون تصویر این معنا را دارند:

- «فایل یا نشانی تصویر»: نام فایل تحویلی یا نشانی مستقیم منبع؛ خود تصویر را داخل سلول قرار نمی‌دهد.
- «متن جایگزین تصویر»: توصیف کوتاه و کاربردی برای دسترس‌پذیری و زمان بازنشدن تصویر.
- «نکته برش»: سوژه یا ناحیه‌ای که در برش‌های موبایل و دسکتاپ باید داخل کادر باقی بماند.

## بازگردانی حساب

مدیر دارای مجوز `users.manage` از «درخواست‌های حساب» وارد فهرست حذف‌ها می‌شود. دکمه «بازگردانی حساب» فقط تا قبل از پایان مهلت سه‌روزه فعال است و تمام عملیات در Audit ثبت می‌شود.
